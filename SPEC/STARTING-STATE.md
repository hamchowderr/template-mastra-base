# Starting State Snapshot

This document captures the exact state of the project when handed off to a local AI coding agent. The scaffold was created via:

```bash
npx create-mastra@latest template-mastra-base \
  --project-name template-mastra-base \
  --default \
  --dir src \
  --components agents,tools,workflows,scorers \
  --llm anthropic \
  --llm-api-key dummy-key-replace-me \
  --no-example \
  --timeout 300000
```

## Files present (excluding node_modules, package-lock.json, SPEC/)

```
.env.example
.gitignore
README.md
package.json
src/mastra/agents/weather-agent.ts
src/mastra/index.ts
src/mastra/scorers/weather-scorer.ts
src/mastra/tools/weather-tool.ts
src/mastra/workflows/weather-workflow.ts
tsconfig.json
```

## package.json (initial)

```json
{
  "name": "template-mastra-base",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "mastra dev",
    "build": "mastra build",
    "start": "mastra start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": { "node": ">=22.13.0" },
  "dependencies": {
    "@mastra/core": "^1.31.0",
    "@mastra/duckdb": "^1.2.0",
    "@mastra/evals": "^1.2.2",
    "@mastra/libsql": "^1.9.1",
    "@mastra/loggers": "^1.1.1",
    "@mastra/memory": "^1.17.4",
    "@mastra/observability": "^1.11.0",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "mastra": "^1.7.3",
    "typescript": "^6.0.3"
  }
}
```

## tsconfig.json (initial; do not modify)

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "outDir": "dist"
  },
  "include": ["src/**/*"]
}
```

## .env.example (initial; will be replaced)

```
OPENAI_API_KEY=your-api-key
```

## src/mastra/index.ts (initial; will be replaced)

Includes weather workflow/agent/scorer registrations and a `MastraCompositeStore` with LibSQL default + DuckDB observability domain. This is the pattern Mastra recommends — keep the composite store structure when rewriting.

## What's missing from the scaffold

- `.env` (must be created from `.env.example`)
- `node_modules/` (run `npm install` first)
- `package-lock.json` (generated by `npm install`)
- Git history (run `git init` if you want one)
- `AGENTS.md` / `CLAUDE.md` (built per spec in Phase 11)
- `Dockerfile` and friends (built per spec in Phase 9)
- `.github/workflows/ci.yml` (built per spec in Phase 10)
- `prompts/` folder (built per spec in Phase 11)
- `scripts/eval.ts` (built per spec in Phase 8)
- `src/lib/env.ts` (built per spec in Phase 3)
- `src/mastra/lib/aimock.ts`, `src/mastra/lib/supabase.ts` (built per spec in Phase 3)
- `src/mastra/agents/_example.ts` (built per spec in Phase 6)
- `src/mastra/scorers/_example.scorers.ts` (built per spec in Phase 5)
- `src/mastra/scorers/datasets/_example.json` (built per spec in Phase 8)

## First steps for the local agent

1. Run `npm install` to install dependencies.
2. Read `SPEC/00-README.md` through `SPEC/06-known-gotchas.md` in order.
3. Begin Phase 0 of `SPEC/04-build-order.md`.
